@inject IAuthService AuthenticationService
@inject NavigationManager Navigation

<nav class="sidebar-menu">

    <NavLink href="/" Match="NavLinkMatch.All">
        <span>🏠</span>
        <span>داشبورد</span>
    </NavLink>

    <NavLink href="/bowls/new">
        <span>🥣</span>
        <span>ثبت کاسه</span>
    </NavLink>

    <NavLink href="/bowls">
        <span>⚙️</span>
        <span>تولید</span>
    </NavLink>

    <NavLink href="/designs">
        <span>🎨</span>
        <span>دیزاین</span>
    </NavLink>

    <AuthorizeView Roles="Administrator,ProductionManager">
    <NavLink href="/scales">
        <span>🎵</span>
        <span>Scale‌ها</span>
    </NavLink>
    </AuthorizeView>

    <NavLink href="/custom-scales">
        <span>🎶</span>
        <span>Scale Custom</span>
    </NavLink>

    <NavLink href="/warehouse/list">
        <span>📦</span>
        <span>انبار سازها</span>
    </NavLink>

    <NavLink href="/warehouse/materials">
        <span>🧱</span>
        <span>انبار مواد اولیه</span>
    </NavLink>

    <NavLink href="/warehouse/export">
        <span>✈️</span>
        <span>انبار صادراتی</span>
    </NavLink>

    <AuthorizeView Roles="Administrator">
    <NavLink href="/admin/production-edit">
        <span>✏️</span>
        <span>ویرایش</span>
    </NavLink>
    </AuthorizeView>

    <AuthorizeView Roles="Administrator,ProductionManager,Marketer">
    <NavLink href="/marketing"><span>📣</span><span>بازاریابی</span></NavLink>
    </AuthorizeView>

    <AuthorizeView Roles="Administrator,ProductionManager,SalesAdmin">
    <NavLink href="/sales"><span>💰</span><span>فروش</span></NavLink>
    <NavLink href="/accounting"><span>🧾</span><span>حسابداری</span></NavLink>

    </AuthorizeView>
    <AuthorizeView Roles="Administrator,ProductionManager">
    <NavLink href="/users/list">
        <span>👤</span>
        <span>کاربران</span>
    </NavLink>

    <NavLink href="/reports">
        <span>📊</span>
        <span>گزارش‌ها</span>
    </NavLink>
    <NavLink href="/reports/payroll"><span>🧮</span><span>دستمزد تولید</span></NavLink>
    <NavLink href="/payroll/exclusions"><span>✅</span><span>دستمزدهای قبلاً محاسبه‌شده</span></NavLink>
    </AuthorizeView>

    <button type="button" class="sidebar-logout" disabled="@_loggingOut" @onclick="LogoutAsync"><span>↪</span><span>@(_loggingOut ? "در حال خروج…" : "خروج از سامانه")</span></button>

</nav>

<div class="sidebar-footer">
    TORSEPAN 1.0
</div>

@code {
    private bool _loggingOut;
    private async Task LogoutAsync(){if(_loggingOut)return;_loggingOut=true;await AuthenticationService.LogoutAsync();Navigation.NavigateTo("/login",true);}
}
